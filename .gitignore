# 环境变量(含密钥,切勿提交;请复制 .env.example 为 .env)
.env
.env.local
*.env

# 编辑器与系统
.DS_Store
.idea/
.vscode/

# 子项目已各自忽略 node_modules/ 与 target/
# 这里补充根目录级别的构建产物与日志
dist/
*.log

# Vite / Vitest 启动时生成的配置缓存,不属于源码
*.timestamp-*.mjs

# 本机 agent 会话笔记,不属于本仓库
.workbuddy-ai/

# 本地文件存储(OSS 未配置时的降级目录,见 backend LocalFileStorage)
backend/data/uploads/
