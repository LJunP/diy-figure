# 03-技术设计说明书

本文档基于 `02-产品需求说明书.md` 的功能模块与状态机反推技术方案，不引入需求文档中未定义的功能。

## 1. 技术栈总览

| 层 | 选型 | 说明 |
|---|---|---|
| 前端 | Vue 3 + Element Plus | 用户端与运营后台同一项目，按路由区分 |
| 后端 | Spring Boot | 单体应用，不做微服务拆分 |
| 数据库 | MySQL + Spring Data JPA | 状态机驱动的关系型数据，JPA减少手写SQL |
| 鉴权 | Spring Security + JWT | 前后端分离场景下无状态鉴权 |
| 对象存储 | 阿里云 OSS | 用户上传图片、AI生成概念图、3D模型文件（.glb） |
| AI 对话/图像 | 通义千问VL 或 智谱GLM-4V（待实测选定）+ 通义万相 | 国内可直连，规避 OpenAI 大陆合规问题 |
| AI 3D生成 | Meshy AI / Tripo3D（待实测选定） | 生成3D参考模型，非直接打印文件 |
| 支付 | 微信支付 + 支付宝 官方 Java SDK | 商户资质待公司主体注册后接入 |
| 部署 | 香港/新加坡境外云服务器 | 无公司主体，规避国内 ICP 备案要求 |
| 定时任务 | Spring `@Scheduled` | 仅用于补购窗口到期检查，不引入消息队列 |

明确不引入：Redis（当前体量不需要缓存层）、WebSocket（站内通知用轮询/页面刷新足够）、微服务网关/服务发现（单体应用不需要）。

## 2. 系统架构图

```
[Vue 3 前端]
  ├─ /                用户端（设计工作台/系列/订单/支付）
  └─ /admin           运营后台（终审/报价/质检/发货/取消处理）
        │  REST API (JWT鉴权)
        ▼
[Spring Boot 单体应用]
  ├─ controller层     接收请求，仅做入参校验与DTO转换
  ├─ service层        业务逻辑，状态机转移规则集中在此层
  ├─ repository层     JPA数据访问
  ├─ integration层    封装外部服务调用（AI、支付、OSS、邮件）
  └─ scheduler        补购窗口到期扫描
        │
        ├──▶ [MySQL]  系列/订单/画布/支付/审计日志等核心数据
        ├──▶ [阿里云OSS] 图片与3D模型文件
        ├──▶ [AI服务]  对话理解 / 2D生成 / 3D生成
        └──▶ [支付网关] 微信支付 / 支付宝（含异步回调接收）
```

## 3. 后端模块划分（Spring Boot 包结构）

```
com.diyfigure
 ├─ config/          安全配置、JWT配置、WebClient配置、OSS配置
 ├─ common/          统一响应体、异常处理、状态机转移规则定义
 ├─ auth/            注册登录、JWT签发与校验
 ├─ series/          系列管理、画布管理、AI设计对话
 ├─ review/          人工终审
 ├─ quote/           报价填写与确认
 ├─ lottery/         抽奖逻辑
 ├─ order/           主状态机推进、地址、发货、质检
 ├─ payment/         定金/尾款/补购支付、微信支付宝回调
 ├─ refill/          补购订单（独立状态机，复用order部分逻辑）
 ├─ cancellation/    取消与违约金计算
 ├─ notification/    站内消息、邮件触发
 └─ admin/           运营后台专用接口（跨模块聚合查询）
```

模块划分原则：**每个模块只负责需求文档里对应的那一个业务能力，模块间通过 service 接口调用，不允许 controller 跨模块直接查库**，避免之前强调的"复杂度扩散、数据流回溯"问题——比如"报价"模块不应该知道"支付"模块的实现细节，只应该在报价确认后，通过一个明确的领域事件或 service 调用通知 order 模块推进状态。

## 4. 核心设计决策：series 与 order 的职责拆分

需求文档里"系列"既承载"设计容器"的含义（画布集合、档位选择），又承载"交易实体"的含义（状态机、报价、支付）。**这两个职责必须拆成两张表**，理由：

- `series` 只负责设计阶段的数据（名称、档位、画布集合），生命周期从创建到设计完成
- `order` 负责从"提交报价申请"开始的全部交易状态机（终审、报价、抽奖、支付、生产、发货）

拆分后，"设计"和"交易"两条关注点分离，series 表不会因为交易状态频繁变化而膨胀，order 表也不需要关心 AI 对话这类与交易无关的细节。**补购订单复用 order 表**（通过 `order_type` 区分 main/refill），因为补购的状态机（待付定金→生产中→待质检→待付尾款→待发货→已完成）是主订单状态机的子集，不需要重复建模。

## 5. 数据库表结构

### 5.1 user（用户表）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | bigint, PK | |
| username / phone / email | varchar | 登录凭证，具体注册方式待定，先都预留字段 |
| password_hash | varchar | |
| role | varchar | USER / ADMIN，V1运营账号也存在此表，用角色区分，不单独建管理员表 |
| created_at | datetime | |

### 5.2 series（系列表，设计容器）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | bigint, PK | |
| user_id | bigint, FK | |
| name | varchar | 用户自命名 |
| spec_tier | varchar | LIGHT(6选4) / CLASSIC(9选6) / COLLECTION(12选8) |
| size_tier | varchar | 对应尺寸档位，具体规格待厂家回填后配置化管理，不硬编码 |
| design_status | varchar | DESIGNING / READY_FOR_QUOTE（已定稿画布数达到档位要求） |
| created_at / updated_at | datetime | |

### 5.3 canvas（画布表，单个角色设计）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | bigint, PK | |
| series_id | bigint, FK | |
| status | varchar | DESIGNING / FINALIZED |
| concept_image_urls | json | 2D三视图（正/侧/背），存OSS地址数组 |
| model_3d_url | varchar | 3D参考模型 .glb 文件OSS地址，定稿后异步生成 |
| model_3d_task_id | varchar, nullable | Meshy 任务 ID，供后台轮询与失败重试 |
| model_3d_status | enum | NONE / PENDING / PROCESSING / SUCCESS / FAILED，见第 8 节异步生成 |
| ai_conversation | json | 对话记录（文字），图片/文件类输入存OSS，此字段存引用+文字内容 |
| finalized_at | datetime | 标记定稿时间 |
| locked | boolean | 定金支付成功后置true，之后不可再编辑，覆盖已中签+未中签 |
| version | bigint | 乐观锁版本号，防止定稿与并发对话同时写入导致状态回退 |

> `model_3d_status` 在 MySQL 上必须是 `enum(...)` 而不是 `varchar`：Hibernate 6 对
> `@Enumerated(EnumType.STRING)` 生成的就是 enum，写成 varchar 会让生产环境的
> `ddl-auto=validate` 启动失败。这个坑在 V3 迁移里踩过一次，已由 V4 修正。

### 5.4 order（订单表，交易状态机，主订单与补购共用）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | bigint, PK | |
| series_id | bigint, FK | |
| user_id | bigint, FK | |
| order_type | varchar | MAIN / REFILL |
| parent_order_id | bigint, FK, nullable | REFILL 类型时指向对应的 MAIN 订单 |
| status | varchar | 见 5.4.1 状态枚举 |
| quoted_price | decimal | 运营填写的报价（MAIN）/ 自动计算价格（REFILL） |
| expected_delivery_date | date | 报价时一并给出 |
| deposit_amount / balance_amount | decimal | 定金/尾款金额，各50%。定金 = 报价÷2 一次舍入，尾款 = 报价 − 定金（减法兜底），保证两者之和精确等于报价 |
| version | bigint | 乐观锁版本号 |
| production_started_at | datetime, nullable | 运营手动标记的"生产实际开工时间"，用于违约金判定，不对用户可见 |
| address_id | bigint, FK, nullable | |
| created_at / updated_at | datetime | |

**5.4.1 status 枚举**（对应02文档第4.2节主状态机）：
`DRAFT_SUBMIT_PENDING`（待提交报价）→ `REVIEWING`（终审中）→ `REVIEW_REJECTED`（终审拒绝）/ `QUOTED`（已报价）→ `CLOSED`（用户拒绝报价）/ `LOTTERY_PENDING`（待抽奖）→ `LOTTERY_DONE`（已抽奖）→ `DEPOSIT_PENDING`（待付定金）→ `IN_PRODUCTION`（生产中）→ `QC_PENDING`（待质检）→ `BALANCE_PENDING`（待付尾款）→ `SHIPPING_PENDING`（待发货）→ `SHIPPED`（已发货）→ `COMPLETED`（已完成）→ `CANCELLED`（已取消）

REFILL 订单状态机是子集，复用同一枚举，跳过 REVIEWING/QUOTED/LOTTERY 系列状态，从 `DEPOSIT_PENDING` 起步。

### 5.5 order_canvas（订单-画布关联表，记录抽奖结果）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | bigint, PK | |
| order_id | bigint, FK | |
| canvas_id | bigint, FK | |
| lottery_result | varchar | SELECTED / NOT_SELECTED |
| refill_available_until | date, nullable | SELECTED角色不设值；NOT_SELECTED角色=主订单发货日+60天 |

### 5.6 payment（支付流水表）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | bigint, PK | |
| order_id | bigint, FK | |
| type | varchar | DEPOSIT / BALANCE |
| channel | varchar | WECHAT / ALIPAY |
| amount | decimal | |
| status | varchar | PENDING / SUCCESS / FAILED |
| channel_transaction_id | varchar | 支付网关返回的交易号，用于核对回调 |
| paid_at | datetime, nullable | |

### 5.7 address（收货地址表）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | bigint, PK | |
| user_id | bigint, FK | |
| receiver_name / phone / detail | varchar | |
| created_at | datetime | |

### 5.8 review_log（人工终审记录）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | bigint, PK | |
| order_id | bigint, FK | |
| reviewer_id | bigint, FK | 指向运营账号 |
| result | varchar | APPROVED / REJECTED |
| reject_reason | varchar, nullable | |
| reviewed_at | datetime | |

### 5.9 quality_check_log（质检记录）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | bigint, PK | |
| order_id | bigint, FK | |
| result | varchar | PASSED / FAILED |
| fail_reason | varchar, nullable | |
| checked_at | datetime | |

### 5.10 cancellation_log（取消/违约记录）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | bigint, PK | |
| order_id | bigint, FK | |
| stage_at_cancel | varchar | 记录取消时所处状态，用于追溯违约金计算依据 |
| deposit_refund_amount | decimal | 退还金额 |
| deposit_penalty_amount | decimal | 扣除金额 |
| cancelled_at | datetime | |

### 5.11 order_status_log（状态变更审计日志）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | bigint, PK | |
| order_id | bigint, FK | |
| from_status / to_status | varchar | |
| operator_type | varchar | USER / ADMIN / SYSTEM |
| operator_id | bigint, nullable | |
| reason | varchar, nullable | |
| created_at | datetime | |

这张表是状态机可追溯性的核心——任何状态转移都必须写入一条记录，不允许直接 UPDATE `order.status` 而不落审计日志，这是 service 层的强制约束，不是可选项。

### 5.12 notification（站内消息表）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | bigint, PK | |
| user_id | bigint, FK | |
| order_id | bigint, FK, nullable | |
| content | varchar | |
| channel | varchar | IN_APP / EMAIL |
| read_at | datetime, nullable | |
| created_at | datetime | |

## 6. 状态机转移规则的实现方式

不引入状态机框架（如 Spring Statemachine），理由：本项目状态转移规则不复杂（无并发状态、无嵌套子状态机的强需求），引入框架会增加不必要的学习成本和抽象层。改用**显式的转移规则映射表 + service层校验**：

```java
// 伪代码示意，展示设计思路而非最终实现
Map<OrderStatus, Set<OrderStatus>> ALLOWED_TRANSITIONS = {
  DRAFT_SUBMIT_PENDING -> [REVIEWING],
  REVIEWING            -> [REVIEW_REJECTED, QUOTED],
  REVIEW_REJECTED      -> [DRAFT_SUBMIT_PENDING],   // 用户修改后重新提交
  QUOTED                -> [CLOSED, LOTTERY_PENDING],
  CLOSED                -> [DRAFT_SUBMIT_PENDING],   // 支持02文档4.5节：可重新打开
  LOTTERY_PENDING       -> [LOTTERY_DONE],
  LOTTERY_DONE          -> [DEPOSIT_PENDING],
  DEPOSIT_PENDING       -> [IN_PRODUCTION, CANCELLED],
  IN_PRODUCTION         -> [QC_PENDING, CANCELLED],
  QC_PENDING            -> [IN_PRODUCTION, BALANCE_PENDING],  // 质检不通过返工
  BALANCE_PENDING       -> [SHIPPING_PENDING],
  SHIPPING_PENDING      -> [SHIPPED],
  SHIPPED                -> [COMPLETED],
}

// service层任何状态转移都必须经过该方法，杜绝绕过校验直接UPDATE
void transition(Order order, OrderStatus to, Operator operator, String reason) {
  if (!ALLOWED_TRANSITIONS.get(order.status).contains(to)) {
    throw new IllegalStateTransitionException(...);
  }
  order.status = to;
  orderStatusLogRepository.save(new OrderStatusLog(order, from, to, operator, reason));
}
```

违约金计算逻辑挂在 `CANCELLED` 转移的前置判断上：根据取消时的 `status` 和 `production_started_at` 是否为空，决定走 02 文档 4.4 节的三档规则中的哪一档。

## 7. API 接口清单（按模块分组，REST风格）

> 约定：所有业务错误也返回 HTTP 200，真实错误码放在响应体的 `code` 字段里，
> 由前端拦截器统一处理；只有鉴权/路由层面的失败才用真实 HTTP 状态码。
> 所有 `/**/admin/**` 路径由 `AdminInterceptor` 做服务端角色校验，
> 前端隐藏按钮不算权限控制。

### 7.1 认证
- `POST /api/auth/register` 注册
- `POST /api/auth/login` 登录（账号字段名为 `account`，支持用户名或邮箱），返回 JWT
- `GET /api/auth/me` 当前登录用户
- `PUT /api/auth/password` 修改密码

### 7.2 系列与画布
- `POST /api/series` 创建系列 · `GET /api/series` 列表 · `GET /api/series/{id}` 详情
- `PUT /api/series/{id}` 更新 · `DELETE /api/series/{id}` 删除（已产生订单则拒绝，409）
- `POST /api/series/{seriesId}/canvases` 新建画布（请求体带 `name`，落库前 trim，空则兜底为「未命名角色」）
- `GET /api/canvases/{id}` 画布详情 · `DELETE /api/canvases/{id}` 删除
- `POST /api/canvases/{id}/chat` AI对话（SSE 流式；**不触发 3D**，3D 只在定稿时入队）
- `POST /api/canvases/{id}/finalize` 定稿（只入队 3D 任务，立即返回，见第 8 节）
- `POST /api/canvases/{id}/model3d/retry` 3D 生成失败后重试（仅 FAILED 可重试）
- `POST /api/canvases/{id}/reopen` 画布退回设计态（同时清空上一版定稿的 3D 状态）
- `POST /api/files/upload-image` 上传参考图（OSS 未配置时落本地磁盘，见第 12 节）

### 7.3 报价与终审
- `GET /api/orders` 我的订单列表 · `GET /api/orders/{orderId}` 订单详情（**同一接口同时服务用户端与运营端**：`getOrderDetail` 内做归属校验，非本人订单时再查 `User.role == ADMIN`，是管理员则放行）
- `POST /api/orders` 创建主订单（校验档位数量与定稿状态，直接进入 REVIEWING）
- `POST /api/orders/{id}/submit-review` **再提交终审**（DRAFT_SUBMIT_PENDING → REVIEWING，返工闭环入口）
- `POST /api/orders/{id}/resubmit` 终审拒绝后回到草稿（REVIEW_REJECTED → DRAFT_SUBMIT_PENDING）
- `POST /api/orders/{id}/reopen` 重开已关闭订单（CLOSED → DRAFT_SUBMIT_PENDING）
- `GET /api/orders/admin/reviews/pending` 待终审列表
- `POST /api/orders/admin/reviews/{orderId}` 终审操作（通过/拒绝+原因）
- `POST /api/orders/admin/orders/{orderId}/quote` 运营填写报价+预计交期
- `GET /api/orders/admin/quotes/pending` 待报价列表 · `GET /api/orders/admin/dashboard/stats` 看板统计
- `POST /api/orders/{id}/accept-quote` 接受报价（报价未填则拒绝）
- `POST /api/orders/{id}/reject-quote` 拒绝报价

> **终审/报价弹窗为什么必须拉订单详情**：`02-产品需求说明书.md` §3.4 要求运营「查看该系列全部定稿设计，判断是否存在侵权风险」，报价同样要看到设计稿才能定价。因此运营端终审（`ReviewList.vue`）与报价（`QuoteList.vue`）弹窗在打开时会额外调用 `GET /api/orders/{orderId}`，用返回的 `canvases[].firstConceptImage` 渲染设计缩略图宫格，并展示系列名/规格档位/设计数量（报价弹窗另有「中签数量」= `lotteryResult == SELECTED` 的计数，中签画布描边高亮为品牌绿）。
>
> 注意 `OrderDetailResponse.CanvasInfo` 只带 **首张** 概念图（`firstConceptImage`），所以宫格是「每张画布一张缩略图」，不是「一张画布的全部概念图」。若要展示多图，需要扩展该 DTO。
>
> 另一个坑：暗色主题下 `el-descriptions` / `el-table` 会出现白底白字，根因是 Element Plus 的 `--el-fill-color-blank` 默认 `#fff` 未被覆盖，已在 `frontend/src/assets/main.css` 置为 `transparent`。

### 7.4 抽奖
- `POST /api/orders/{id}/lottery/draw` 执行抽奖（必须用户手动触发，不自动执行）

### 7.5 地址与支付
- `GET/POST /api/addresses` · `GET/PUT/DELETE /api/addresses/{id}`
- `POST /api/orders/{id}/address` 绑定收货地址（同时计算定金/尾款）
- `POST /api/payments/orders/{orderId}/deposit` 创建定金支付单
- `POST /api/payments/orders/{orderId}/balance` 创建尾款支付单
- `GET /api/payments/orders/{orderId}` 支付流水
- `POST /api/payments/{paymentId}/simulate` **模拟支付成功**（仅非 prod 且 `payment.mock-enabled=true` 时可用）
- `POST /api/payments/callback/wechat` · `POST /api/payments/callback/alipay` 真实支付回调入口

### 7.6 生产与质检（运营端）
- `POST /api/admin/orders/{orderId}/production-start` 标记生产实际开工（违约金分档依据）
- `POST /api/orders/admin/orders/{orderId}/submit-qc` 送检
- `POST /api/admin/orders/{orderId}/quality-check` 质检结果录入（不通过则返工回生产中）
- `POST /api/admin/orders/{orderId}/ship` 发货（同时开启未中签角色 60 天补购窗口）
- `POST /api/admin/orders/{orderId}/complete` 运营标记完成
- `GET /api/admin/orders/{orderId}/qc-logs` 质检记录
- `POST /api/orders/{orderId}/confirm-received` 用户确认签收

### 7.7 补购
- `GET /api/orders/{orderId}/refill/available` 可补购角色列表（含是否过期、自动算价）
- `POST /api/orders/{orderId}/refill` 发起补购
- `GET /api/refills` 我的补购订单

### 7.8 取消与违约
- `POST /api/orders/{orderId}/cancel` 用户取消（按**实付定金**自动判定违约金档位）
- `GET /api/admin/cancellations` 运营端违约记录查询

### 7.9 通知
- `GET /api/notifications` 站内消息列表
- `GET /api/notifications/unread-count` 未读数
- `PUT /api/notifications/{id}/read` 标记单条已读（校验归属）
- `PUT /api/notifications/read-all` 全部标记已读

**消息从哪来**：不由用户主动触发，而是状态机旁路——`OrderStateMachineService` 每次状态转移都会调用 `NotificationService.notifyOrderStatusChanged(orderId, userId, toStatus)`：
- 站内消息：`STATUS_MESSAGE` 覆盖 14 个状态节点，文案即用户可读的下一步指引。
- 邮件：仅 `QUOTED`（报价已确认）、`BALANCE_PENDING`（需付尾款）、`SHIPPED`（已发货）三个节点发送，与 `02-产品需求说明书.md` 一致。

**两条硬约束**：
1. 通知是旁路能力，任何异常都不能阻断订单主流程——状态机侧捕获异常，`NotificationService` 自身也不向外抛业务异常。
2. 邮件必须**事务提交后**才发（`EmailService.sendAfterCommit` 注册 `TransactionSynchronization`），否则会出现「事务回滚了但邮件已经发出去」。
3. 未配置 SMTP 时降级为只记日志。注意不能只用「`JavaMailSender` bean 存不存在」判断：`application.yml` 里 `spring.mail.host` 写成 `${SPRING_MAIL_HOST:}`，未配时是**空字符串**，而 Spring Boot 的 `MailSenderCondition` 只判断属性是否存在，空串也算存在，于是 bean 照常创建、每次通知白连一次 SMTP 再失败。`EmailService` 因此额外判了一次 `host` 是否非空。

## 8. 定时任务

- `RefillExpiryScheduler`：每日凌晨 2:00 扫描 `order_canvas` 表中 `refill_available_until` 已过期但未补购的记录，**只统计并打日志，不修改任何状态**——过期即不可补购是由 `refill_available_until` 字段本身的语义表达的，真正的拦截在 `RefillService` 创建补购单时做日期检查（`refill_available_until.isBefore(today)` 则拒绝）。补购是非强制功能，过期静默失效，不发送通知。
- `Model3dTaskService`：每 15 秒推进一次 3D 参考模型的生成任务。
  定稿时不再同步轮询 Meshy —— 旧实现最长阻塞 5 分钟，会撑爆前端 30 秒超时。
  现在定稿只写 `PENDING` 就立即返回，由它提交任务（每轮最多 3 个）并轮询处理中的任务
  （每轮最多 10 个），成功写回 `model_3d_url`，失败置 `FAILED` 让用户手动重试。

**调度总开关**：两个定时任务的方法体开头都会读 `diy.scheduling.enabled`（默认 `true`）。
集成测试把它置为 `false`，否则调度线程会在 Flyway `clean()` 重建表的过程中查表并报错。

> ⚠️ 不要用 `spring.task.scheduling.enabled` 来做这件事 —— 它**不是** Spring Boot 的真实属性，
> 配了也不生效，调度线程照样起来。这个坑已经在 `application-test.yml` 的注释里写明。

## 9. 数据库迁移与一致性守卫

表结构以 `backend/src/main/resources/db/migration` 下的 Flyway 脚本为准，不再依赖 Hibernate 自动建表：

| 版本 | 内容 |
|---|---|
| V1 | 全量初始建表（12 张表），结构取自开发环境 `SHOW CREATE TABLE` 的真实结果 |
| V2 | `order / order_canvas / payment / canvas / series` 增加 `version` 乐观锁字段 |
| V3 | `canvas` 增加 `model_3d_task_id` / `model_3d_status`（异步 3D 生成） |
| V4 | 修正已迁移库上 `model_3d_status` 的列类型（varchar → enum） |
| V5 | `canvas` 增加 `name`（角色名称，NOT NULL），存量数据回填为「角色 {id}」 |
| V6 | `user.email_verified` / `enabled`；`auth_token`；支付退款记账列；抽奖 `UNDECIDED`；地址 `is_default` |

- 开发环境 `ddl-auto=update` + `baseline-on-migrate`，对已有库自动打基线，不重复执行 V1
- **生产环境 `ddl-auto=validate`**：Flyway 建出来的表与 JPA 实体对不上就直接启动失败，
  宁可起不来也不能带着错的表结构跑
- `spring.flyway.clean-disabled` 在生产配置里显式关掉 clean

**并发一致性**：`order`、`order_canvas`、`payment`、`canvas`、`series` 均带 `@Version` 乐观锁，
覆盖"定稿与并发对话同时写"、"并发支付回调"、"并发提交报价"这几个真实会撞在一起的场景。

**回归测试**：`OrderFlowIntegrationTest` 用独立 schema `diy_figure_it`，
每个用例前 Flyway clean + migrate 重建，串起来跑主订单、返工、取消三档、补购、过期、
关闭重开、质检返工、通知、越权、删除保护等分支。它同时充当"迁移脚本与实体是否一致"的守卫。
`CanvasLifecycleIntegrationTest` 专盯设计链路（定稿异步入队 3D、重新打开清空 3D、重试准入、
画布名称落库、删除保护、系列设计状态联动），`CrossUserAuthorizationTest` 盯越权与文件上传降级通道。

## 10. 部署方案

```
香港/新加坡云服务器（单机部署，规避国内ICP备案要求）
  ├─ Nginx：反向代理 + 托管 Vue 打包后的静态资源
  ├─ Spring Boot 应用（Jar包直接运行，或用 systemd 托管进程）
  └─ MySQL（同机部署，月10单体量无需独立数据库实例）

CI：GitHub Actions（.github/workflows/ci.yml）
    ├─ backend：MySQL 8 service 起一个 diy_figure_it 空库，跑 mvn test
    └─ frontend：npm ci + npm run build
    —— 部署仍然手动（git pull + 重新构建 + 重启），CI 只负责"别把坏代码合进来"

容器化：backend/Dockerfile 已提供多阶段构建镜像，非 root 用户运行；
        docker-compose.yml 用于本地一键起 MySQL + 后端，不是生产编排方案
```

## 11. 待厂家/供应商信息回填后需要调整的部分

- `series.size_tier` 目前是自由字符串，待包装盒规格确定后改为配置表驱动（枚举值从数据库配置读取，不硬编码在代码里，方便调整）
- `order.quoted_price` 的运营填写目前是纯人工输入，无参考基准；待拿到厂家报价区间后，可在运营端报价页面增加"建议价格区间"提示（不做自动定价，报价决策权仍在人工）

## 12. 文件存储的两条通道

| 通道 | 触发条件 | 行为 |
|---|---|---|
| 阿里云 OSS | 配置了 `OSS_ACCESS_KEY_ID` / `OSS_ACCESS_KEY_SECRET` | 上传到 OSS，返回 `{domain}/{objectKey}` |
| 本地磁盘 | OSS 密钥仍是占位值 | `LocalFileStorage` 落盘到 `diy.local-storage.dir`，返回 `{publicBaseUrl}/uploads/...` |

**为什么要有本地通道**：早期实现在 OSS 未配置时直接丢弃上传的参考图、固定返回一张
`placehold.co` 的示意图。结果是"用户上传了参考图，但对话里看到的是别人的图"——
Demo 流程自相矛盾，也无法验证上传链路本身是否正常。现在改为落盘，返回真实可访问的 URL。

关键实现点：

- 两条通道共用 `OssService.validateImageType()`，避免"配了 OSS 会被拦、没配 OSS 却能传"的安全缺口
- `WebMvcConfig` 把 `/uploads/**` 映射到本地目录，并把该路径加入 JWT 白名单
  （图片 URL 含随机 UUID，且浏览器 `<img>` 不会带 `Authorization` 头）
- `LocalFileStorage.store()` 做目录穿越校验，扩展名只保留 `[A-Za-z0-9]{1,8}`
- URL 前缀优先取 `diy.public-base-url`，留空（默认）时按**当前请求**推导 ——
  这样返回的链接天然带真实的 host、端口和 context-path，不会出现"服务跑在 8088、
  返回的却是 8080 链接"这种配置漂移。只有挂在反向代理后面、请求头 host 不是最终对外域名时
  才需要显式配置（且必须含 context-path，例如 `https://api.your-domain.com/api`）
- 生产环境配好 OSS 后本地通道不会被走到，可用 `DIY_LOCAL_STORAGE_ENABLED=false` 彻底关闭

> 注：`OssService.transferFromUrl()`（AI 生成图片转存）在 OSS 未配置时仍返回原始 URL，
> 不做本地下载 —— 避免为一张占位图引入额外的外网依赖和超时风险。
