# 02-产品需求说明书

## 1. 产品概述

用户通过 AI 对话式工具设计原创角色形象，以"系列"为单位组织成一套盲盒，提交平台报价审核，通过抽奖机制确定实际生产的角色子集，经支付、生产、质检、发货流程，最终获得实体定制盲盒手办。

## 2. 用户角色

| 角色 | 说明 |
|---|---|
| 普通用户 | 注册登录后可创建系列、设计画布、提交报价、支付、查看订单状态、补购 |
| 平台运营（管理端） | 人工终审设计合规性、填写报价与预计交期、质检确认、录入发货信息、处理取消/违约 |

V1 仅这两类角色，不设厂家侧账号（厂家通过线下/站外方式对接，不接入系统）。

## 3. 功能模块划分

### 3.1 账号体系
- 注册/登录（账号密码；邮箱验证与忘记密码已实现。第三方登录不进入 V1，需要 OAuth 应用凭证后再定）
- 个人中心：查看历史系列、订单、补购记录

### 3.2 系列管理
- 创建系列：命名、选择规格档位（轻量/经典/收藏）、选择尺寸档位
- 系列内画布列表：展示所有画布及各自状态（设计中/已定稿）；每个画布显示用户创建时填写的**角色名称**（不填则记为「未命名角色」），订单详情与补购列表沿用同一名称
- 系列整体状态展示（设计中/待抽奖/已抽奖/待报价/已报价/生产中/已完成等，详见第4节状态机）

### 3.3 AI 设计工作台（画布）
- 左侧：AI 对话窗口，支持文字、图片、文件输入
- 右侧：实时展示台，显示 AI 根据对话生成的设计（2D 概念图三视图，定稿后追加 3D 参考模型可旋转预览）
- 画布内可无限次迭代对话修改，直到用户手动标记"已定稿"
- 对话本身不生成 3D：3D 用的是定稿那一刻的第一张概念图，设计还在迭代时生成既无意义又浪费外部配额
- 已定稿画布在设计锁定前可"重新打开"回到设计态；重新打开会**清空上一版定稿生成的 3D 模型**，改完需重新定稿并重新生成
- AI 层面的合规拦截：system prompt 约束禁止生成可辨识真实人物/已有 IP 角色，触发时 AI 应引导用户转向原创方向而非直接生成

### 3.4 报价申请与人工终审
- 提交条件：系列内"已定稿"画布数量达到所选档位要求的设计总数（如收藏档需 12 个已定稿画布）
- 提交后触发人工终审：运营查看该系列全部定稿设计，判断是否存在侵权风险
  - 终审不通过：拒绝报价，注明原因，用户可修改画布后重新提交
  - 终审通过：进入人工报价环节，运营填写价格与预计交期，一并反馈给用户

### 3.5 抽奖模块
- 用户接受报价后触发抽奖：从"已定稿"的设计总数中，按档位比例随机抽取实际生产的子集（如 12 选 8）
- 抽奖结果不可重抽，中签角色列表与未中签角色列表分别展示

### 3.6 支付模块
- 抽奖完成后：强制填写收货地址（不填不可继续）→ 支付定金（50%）
- 定金支付成功：设计锁定，不可再修改任何已中签角色的画布内容
- 生产完成、平台质检通过后：触发尾款（50%）支付请求 → 用户支付尾款
- 尾款支付成功：进入发货流程
- 支付渠道：微信支付、支付宝（国内）；国际化支付渠道 V1 不做

### 3.7 订单与生产跟踪
- 订单状态可视化（状态机详见第4节）
- 运营端：录入厂家生产进度、质检结果、发货物流单号
- 用户端：查看当前状态、预计交期、物流单号

### 3.8 补购模块
- 主订单发货后 60 天内，未中签角色可发起补购
- 补购价格自动计算：`(系列套餐总价 ÷ 中签数量) × 1.3`，无需重新走人工报价
- 补购走相同的定金+尾款支付流程，跳过 AI 设计与终审环节（设计已定稿过审）

### 3.9 取消与违约处理
- 未付定金前取消：无损失，直接取消
- 已付定金、生产未开始取消：扣除部分定金（比例待运营端配置，参考值 30%）作为设计与报价服务费，剩余退还
- 已进入生产阶段取消：定金不退

### 3.10 通知模块
- 站内消息：全量覆盖所有状态变化
- 邮件通知：仅在报价确认、需付尾款、已发货三个关键节点触发

### 3.11 后台管理（运营端）
- 系列/订单列表与详情查看
- 人工终审操作界面（查看设计图、通过/拒绝）
- 报价填写界面（价格、预计交期）
- 质检确认与发货信息录入
- 取消/违约处理与定金扣款记录

## 4. 核心状态机

### 4.1 画布状态

```
设计中 → 已定稿
（已定稿画布在设计锁定前，用户仍可重新打开修改，
  修改后需重新标记"已定稿"）
```

### 4.2 系列/订单状态（主状态机）

```
草稿（系列创建，画布设计中）
  ↓（已定稿画布数达到档位要求）
待提交报价
  ↓（用户点击"提交终审"）
终审中
  ↓——终审不通过——→ 终审拒绝（用户修改画布后可重新提交，回到"待提交报价"）
  ↓——终审通过
已报价（运营填写价格 + 预计交期）
  ↓——用户拒绝报价——→ 已关闭
  ↓——用户接受报价
待抽奖
  ↓（系统按比例执行抽奖）
已抽奖
  ↓（用户填写收货地址）
待付定金
  ↓（支付成功，设计锁定）
生产中
  ↓（厂家完成生产，货送至平台）
待质检
```

> 注1：主状态机对外只展示单一"生产中"状态，但订单数据需内部记录"生产实际开工时间"字段（由运营手动标记，不作为独立可见状态），用于第4.4节违约金分级判定"生产是否已实际开始"，避免因状态合并丢失该判定依据。
>
> 注2："待提交报价"是一个真实的停留态，不是中间态。终审被拒、或用户拒绝报价后重开订单，都会回到这里，用户改完设计后**必须再点一次"提交终审"**才会重新进入运营的终审队列。提交时会重新校验画布数量与定稿状态，不满足则拒绝提交，避免把不合规设计重新塞进审核队列。

```
  ↓——质检不通过——→ 返厂返工（回到"生产中"）
  ↓——质检通过
待付尾款
  ↓（支付成功）
待发货
  ↓（平台转发发货，录入物流单号）
已发货
  ↓（用户签收，或物流签收回执）
已完成
```

### 4.3 补购订单状态（独立于主订单）

```
可补购（未中签角色，60天窗口内）
  ↓（用户发起补购，系统自动计算价格，用户确认后支付）
待付定金
  ↓（支付成功）
生产中（复用已定稿设计，运营下单给厂家）
  ↓
待质检（货回平台，运营质检）
  ↓——质检不通过——→ 返厂返工
  ↓——质检通过
待付尾款
  ↓（支付成功）
待发货 → 已发货 → 已完成
```

> 补购跳过 AI 设计、终审、抽奖环节，但生产、质检、定金+尾款支付流程与主订单相同。

### 4.4 取消/违约分支（可在多个主状态节点触发）

```
待提交报价 / 终审中 / 已报价 / 待抽奖（未付定金）
  → 用户取消 → 已取消（无扣款）

生产中（运营尚未实际向厂家下单/厂家尚未开工，依据订单内部"生产实际开工时间"字段判定）
  → 用户取消 → 已取消（扣除**实付定金**的 30% 作服务费，剩余 70% 退还）

生产中（厂家已实际开工）/ 待质检 / 待付尾款 / 待发货
  → 用户取消 → 已取消（定金不退，如已付尾款需协商退款方案，V1 默认不支持此阶段取消退款）
```

### 4.5 边界条件与未定义场景说明

| 场景 | 规则 |
|---|---|
| 用户拒绝报价后"已关闭"系列 | 系列不永久关闭；用户可重新打开，修改画布或重新提交报价申请（价格重新谈）；运营可据实重新报价 |
| "已发货"如何流转到"已完成" | V1：用户手动确认收货；或运营人工标记；无自动超时关单机制（月10单体量可人工管控） |
| 已定稿画布的锁定范围 | 定金支付成功后，**已中签和未中签画布全部锁定**，不可修改；原因：未中签画布代表已过审的潜在补购品，允许修改会重新触发合规审核链路，增加复杂度 |
| 补购的质检不通过处理 | 返厂返工，不重新触发定金支付；补购定金在质检通过后仍作为定金抵扣尾款 |
| 终审拒绝后的返工闭环 | 终审拒绝 → 用户修改 → "重新提交"（回到待提交报价）→ 用户点"提交终审"（重新进入终审队列）。两步缺一不可，订单不会自动回到终审 |
| 3D 参考模型生成失败 | 定稿不再同步等待 3D 生成（旧实现最长阻塞 5 分钟，会撑爆前端超时）。改为后台异步生成，画布上记录生成状态：生成中 / 成功 / 失败（失败可手动重试）。3D 模型只是给厂家原型师的精修参考，失败不阻塞订单流转 |
| 金额拆分精度 | 定金 = 报价 ÷ 2 一次舍入，尾款 = 报价 − 定金（减法兜底），保证"定金 + 尾款 == 报价"，不会出现 1000.01 被拆成 500.01 + 500.01 |

## 5. 关键业务规则汇总

| 规则项 | 定义 |
|---|---|
| 规格档位 | 轻量（6选4）/ 经典（9选6）/ 收藏（12选8），保留比例约 2/3 |
| 尺寸档位 | 对应不同大小包装盒，盒身外观统一设计，仅尺寸不同 |
| 定金比例 | 50%，接受报价+抽奖完成后收取，收取后设计锁定 |
| 尾款比例 | 50%，厂家生产完成、平台质检通过后收取，收到后才发货 |
| 违约金 | 分三档，基数都是**实付定金**而非订单总价：未付定金无损失 / 已付定金未开工扣定金 30%、退 70% / 已开工不退定金 |
| 补购窗口 | 主订单发货后 60 天 |
| 补购定价 | (系列套餐总价 ÷ 中签数量) × 1.3 |
| 合规红线 | 禁止设计模仿真实明星/公众人物/已有版权 IP 角色，AI自审 + 人工终审双重拦截 |
| 终审时机 | 用户点击"提交报价申请"时触发，不对每次 AI 对话生成都审核 |
| 质检流程 | 厂家生产完成后货物先回平台质检，平台质检通过后才收尾款、才转发用户 |
| 地址填写时机 | 抽奖完成后、支付定金前，强制填写，不填不可继续支付 |
| 通知渠道 | 站内消息全量；邮件仅在报价确认/需付尾款/已发货三个节点 |
| 国际化 | V1 不支持国际发货与 Stripe 支付，延后至后续迭代 |

## 6. 页面清单（用户端）

1. 首页/引导页
2. 登录/注册页
3. 个人中心（系列列表、订单列表、补购记录）
4. 创建系列页（命名、规格档位选择、尺寸档位选择）
5. 系列详情页（画布列表 + 各画布状态 + 提交报价入口）
6. AI 设计工作台页（左：对话窗口；右：实时展示台，含 2D 三视图与 3D 预览切换）
7. 报价结果页（价格 + 预计交期 + 接受/拒绝操作）
8. 抽奖页（抽奖交互 + 结果展示：中签/未中签列表）
9. 收货地址填写页
10. 支付页（定金/尾款，微信支付/支付宝）
11. 订单状态跟踪页（当前状态、物流信息）
12. 补购页（未中签角色列表 + 自动计算价格 + 支付）
13. 消息中心页（站内消息列表、未读角标、单条/全部标记已读）
14. 用户协议与合规说明页（含免责条款）

## 7. 页面清单（运营后台）

1. 登录页
2. 运营看板页（待终审、待报价、生产中、待质检、待发货等各状态数量统计）
3. 系列/订单列表（可按状态筛选）
4. 终审操作页（查看设计图、通过/拒绝并注明原因）
5. 报价填写页（价格、预计交期）
6. 生产进度录入页（可查看历史质检记录）
7. 质检确认页（通过/不通过并注明原因）
8. 发货信息录入页（物流单号）
9. 取消/违约处理页（扣款记录）
10. 补购订单管理页

## 8. 待厂家对接后回填的参数（当前为空白，不阻塞产品设计，但阻塞正式报价上线）

- 各档位报价基准区间
- 生产周期天数（用于"预计交期"字段的默认值）
- 包装盒实际尺寸规格（对应各尺寸档位）
- 厂家可接受的设计交付格式确认（2D 三视图 + 3D 参考模型 .glb）
